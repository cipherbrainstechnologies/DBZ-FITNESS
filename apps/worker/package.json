{
  "name": "@saiyan/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Saiyan Ascend BullMQ worker (health + SIMULATED notification intents)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=../../.env dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@saiyan/database": "workspace:*",
    "@saiyan/domain": "workspace:*",
    "bullmq": "5.44.0",
    "ioredis": "5.6.0",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@types/node": "22.13.10",
    "tsx": "4.19.3",
    "typescript": "5.7.3"
  }
}
