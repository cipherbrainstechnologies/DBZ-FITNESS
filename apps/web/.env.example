# Browser traffic uses same-origin /api/v1 (Next.js BFF).
# Set API_PROXY_TARGET for the server-side proxy destination.
API_PROXY_TARGET=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
